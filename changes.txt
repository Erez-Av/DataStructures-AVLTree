ADDED FILES:
our_tester.py
changes.txt

CHANGES:
AVLTree.py:
12.6.26
- implement init and is_real_node(), and added fields successor,predecessor,bf (and are mainted for now) in AVLNode
- implemented (not completely) the function init, search, insertion, get_height, avl_to_list, size, get_root in AVLTree
- added fields tsize, virtual_node in AVLTree
- added function find_succ, find_pred, update_bf, rolls (in progress) and __repr__

15.6.26
- finished insert and delete
- deleted fields successor and predecessor, and function related to them
- finished roll function
- added comments

18.6.26
- insert now returns correct values (need more checks)
- fixed bug in rolls when updating nodes height and bf, and removed unnecessary arguments and return values
- replaced updae_height and update_bf with update_node (does both so no need for 2 functions)

05.07.26
- made changes to code to make it more readable and "correct", and added some comments
- deleted unnecessary functions create_virtual_childs and get_num_children
- edited roll function to not take roll_count as parameter as it is unnecessary
- added a condition to "avl_to_list" to handle the case where tree is empty as it wouldnt work on an empty tree
